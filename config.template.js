// This file IS committed to git — it's just a template with no real secrets.
// For local testing: copy this file to config.js (same folder) and fill in real values there.
// config.js itself is gitignored and never committed.
// On Vercel, config.js is generated automatically at build time from Environment Variables —
// see generate-config.js.
window.APP_CONFIG = {
  FIREBASE_API_KEY: "PASTE_YOUR_FIREBASE_API_KEY",
  GEMINI_API_KEY: "PASTE_YOUR_GEMINI_API_KEY",
  DAILY_API_KEY: "PASTE_YOUR_DAILY_API_KEY",
  DAILY_SUBDOMAIN: "PASTE_YOUR_DAILY_SUBDOMAIN"
};